# Cách 1: Truy cập trực tiếp
docker exec -it mysql mysql -uroot -pvtat2011

# Cách 2: Truy cập qua bash
docker exec -it mysql bash
mysql -uroot -pvtat2011
-- Xem danh sách databases
SHOW DATABASES;

-- Chọn database để làm việc
USE testflutter;

-- Xem danh sách bảng
SHOW TABLES;

-- Xem cấu trúc của bảng
DESCRIBE table_name;
DESC table_name;

-- Xem dữ liệu trong bảng
SELECT * FROM table_name;

-- Xem dữ liệu với điều kiện
SELECT * FROM table_name WHERE column_name = 'value';

-- Đếm số bản ghi
SELECT COUNT(*) FROM table_name;

-- Xem trạng thái của MySQL
STATUS;

-- Xem version của MySQL
SELECT VERSION();